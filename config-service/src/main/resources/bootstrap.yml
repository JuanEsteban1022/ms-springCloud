server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JuanEsteban1022/ms-springCloud.git
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
          search-paths: config-data
          default-label: eureka-server
  #        bootstrap: true

  security:
    user:
      name: root
      password: s3cr3t